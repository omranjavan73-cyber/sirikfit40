# راهنمای جامع معماری و عیب‌یابی سیستم SIRIK FIT (SIRIKFIT Guide)

این راهنما شامل توضیحات کامل معماری جریان داده محلی، نحوه کارکرد استخراج‌کننده هوشمند لینک محصولات (Dr. Nutrition, Life Pharmacy, GNC, Amazon)، و دستورالعمل‌های عیب‌یابی در محیط Firebase Cloud Functions می‌باشد.

---

## ۱. معماری جریان داده و همگام‌سازی محلی (Local-First Architecture)

برای جلوگیری از بازگشت قیمت درهم به مقادیر پیش‌فرض (مانند ۵۵,۰۰۰ تومان) در صورت قطعی یا تاخیر سرور، سامانه از الگوی **محتوای محلی به عنوان منبع اصلی حقیقت** استفاده می‌کند:

### زنجیره اولویت منبع حقیقت (Source of Truth Hierarchy):
1. **کلید مستقیم نرخ درهم (`sirikfit_aed_rate`)**: آخرین نرخ ذخیره‌شده توسط مدیر در مرورگر.
2. **تنظیمات مالی (`sirikfit_financial_settings`)**: شامل نرخ کرایه حمل، درصد سود، و نرخ درهم.
3. **تنظیمات پوسته و CMS (`sirikfit_cms_config`)**: شامل نرخ‌های دستی ثبت‌شده در CMS.
4. **همگام‌سازی ابری (Firestore)**: فراخوانی پس‌زمینه بدون اورراید کردن داده‌های محلی معتبر.
5. **مقدار پیش‌فرض سیستم (Fallback)**: در صورت خالی بودن کامل حافظه محلی.

### الگوی ذخیره‌سازی اتمی (Atomic Save Sequence):
هنگامی که مدیر نرخ درهم یا تنظیماتی را ذخیره می‌کند، مراحل زیر انجام می‌شود:
```
[ ورودی مدیر ] ──> [ تبدیل رشته به عدد (Number/parseFloat) ] ──> [ ذخیره اتمی در LocalStorage ]
                                                                             │
┌────────────────────────────────────────────────────────────────────────────┘
▼
[ رویداد همزمان window.dispatchEvent('settingsUpdated') ] ──> [ بروزرسانی آنی <Header /> و UI ]
│
▼
[ فراخوانی غیرهمزمان (Async) به Firestore و /api/settings ]
```

---

## ۲. موتور استخراج لینک و استخراج‌کننده محصولات (Product Link Scraper)

سیستم استخراج لینک محصولات از فروشگاه‌های دبی (مانند **Dr. Nutrition**, **Life Pharmacy**, **GNC**, **Sporter**, **Amazon AE**) دارای ۴ لایه استخراج و لایه‌بندی پشتیبان است:

### لایه‌های استخراج:
1. **سرور بک‌اند `/api/parse-link`**: استفاده از ScraperAPI و متاتگ‌های OpenGraph.
2. **سرویس Microlink API**: استخراج آنی متاتگ‌ها و تصاویر کیفیت بالا با عبور از چالش‌های Cloudflare.
3. **پروکسی‌های CORS و Jina AI Reader**: دریافت HTML و استخراج متاتگ‌های محصول.
4. **استخراج هوشمند Gemini AI با چرخش کلیدها (Key Rotation)**: تجزیه محتوای صفحه و بازگرداندن JSON ساختاریافته شامل عنوان دوزبانه، قیمت درهم، تصویر، و وزن محصول.

### فروشگاه‌های پشتیبانی‌شده ویژه:
- **Dr. Nutrition (`drnutrition.com`)**: استخراج عنوان دوزبانه، قیمت درهم (AED)، و تصویر اصلی.
- **Life Pharmacy (`lifepharmacy.com`)**: استخراج تخفیف‌ها و قیمت درهم.
- **GNC UAE (`gnc-mena.com`)**: استخراج مکمل‌های ورزشی و برند.

---

## ۳. امنیت اجرا در محیط ابری Firebase Cloud Functions (حل خطای EROFS 500)

محیط اجرا در Firebase Cloud Functions و Cloud Run به‌صورت **Read-Only** می‌باشد. نوشتن در پوشه `./data` باعث بروز خطای fatal ۵۰۰ (`EROFS: read-only file system`) می‌شد.

### راهکار پیاده‌سازی‌شده:
- **تشخیص محیط ابری**:
  ```typescript
  const isCloudEnv = !!(process.env.K_SERVICE || process.env.FUNCTION_TARGET || process.env.GAE_ENV);
  const DATA_DIR = isCloudEnv ? path.join('/tmp', 'data') : path.join(process.cwd(), 'data');
  ```
- **اندپوینت‌های ایزوله‌شده آنالیتیکس (`/api/analytics/track-visit`)**:
  تمامی فراخوانی‌های ذخیره دیسک در بلوک‌های `try/catch` قرار گرفته و در محیط ابری از دیتابیس Firestore یا حافظه موقت (In-memory) استفاده می‌کنند و همواره پاسخ 200 OK ارسال نمایند.

---

## ۴. دستورالعمل عیب‌یابی (Troubleshooting)

### مشکـل ۱: نرخ درهم پس از رفرش صفحه تغییر می‌کند
- **علت**: پاک شدن حافظه محلی یا عدم فراخوانی `saveAdminSettingsPayload`.
- **حل**: در مرورگر (DevTools -> Application -> Local Storage) کلید `sirikfit_aed_rate` را بررسی کنید. باید عدد معتبر (مثلاً `51200`) ثبت شده باشد.

### مشکـل ۲: استخراج لینک محصول خطا می‌دهد
- **علت**: مسدود شدن IP یا تغییر ساختار HTML فروشگاه.
- **حل**: کلیدهای Gemini API را در پنل مدیریت بخش تنظیمات هوش مصنوعی وارد کنید تا استخراج Gemini به‌صورت خودکار لینک را تحلیل و قیمت را استخراج کند.

---
*SIRIK FIT Architecture & Maintenance Guide — Updated 2026*
