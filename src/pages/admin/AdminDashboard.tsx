import React from 'react';
import { AdminPanel } from '../../components/AdminPanel';

export const AdminDashboard: React.FC<any> = (props) => {
  return <AdminPanel {...props} />;
};

export default AdminDashboard;
